<% @title = "Edit Newsletter" %>

<% form_for :newsletter do |f| %>

  Subject:<br />
  <%= f.text_field :subject %><br />
  Body:<br />
  <%= f.text_area :body %><br />
  <%= hidden_field_tag :id, params[:id] %>
  <%= submit_tag 'Save' %>

<% end %>